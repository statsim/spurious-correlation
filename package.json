{
  "name": "spurious",
  "version": "0.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "browserify src/main.js -s Spurious | uglifyjs -cm > spurious.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anton Zemlyansky",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.5.1",
    "uglify-es": "^3.3.9"
  },
  "dependencies": {
    "dygraphs": "^2.1.0",
    "ml-regression-simple-linear": "^2.0.2",
    "node-correlation": "^1.0.2",
    "random": "^2.2.0"
  }
}
